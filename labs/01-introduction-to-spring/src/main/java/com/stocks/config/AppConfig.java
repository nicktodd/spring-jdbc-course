package com.stocks.config;

import org.springframework.context.annotation.ComponentScan;
import org.springframework.context.annotation.Configuration;

/**
 * AppConfig is the Spring configuration class.
 *
 * TODO:
 *   Add the @Configuration and @ComponentScan("com.stocks") annotations to this class.
 *
 *   @Configuration marks this as a Spring configuration class.
 *   @ComponentScan("com.stocks") tells Spring to scan the com.stocks package tree
 *   and register any class annotated with @Service, @Repository, or @Component as a bean.
 */
public class AppConfig {
    // No additional code needed - annotations do all the work.
}
