package com.stocks.service;

import com.stocks.model.Stock;

import java.util.List;
import java.util.Optional;

/**
 * StockService defines the business operations for stocks.
 *
 * TODO: Define the following method signatures:
 *
 *   List<Stock> getAllStocks()
 *   Optional<Stock> getStockById(Long id)
 *   Stock addStock(Stock stock)
 *       This method should enforce the business rule that duplicate symbols
 *       are not allowed.
 */
public interface StockService {

    // TODO: add method signatures here

}
